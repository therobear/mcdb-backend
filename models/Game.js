/**
 * @typedef Game
 * @property {integer} id
 * @property {string} name
 * @property {string} coverUrl
 */
module.exports = function Game(game) {
    this.id = game.id;
    this.name = game.name;
    this.coverUrl = coverUrl;
};
